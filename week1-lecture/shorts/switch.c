#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int x = get_int("Type: ");
    switch(x)
    {
        case 1:
        printf("One!\n");
        break;
        //if you don't break between every case, the program will execute every case below the first one that actived.
        //If the user types 2, without the break, the program will print Two! Three! Sorry!

        case 2:
        printf("Two!\n");
        break;

        case 3:
        printf("Three!\n");
        break;

        default:
        printf("Sorry!\n");
    }


    //question mark column, also known as ternary operator (?). It simulates an if/else.
    int = y
    if (expression)
    {
        y = 5;
    }
    else
    {
        y = 6;
    }

    int y = (expression) ? 5 : 6;


    do
    {

    }
    while(); //it will run at least one time. If the expressesion in the

}
